PORT=3000

REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# Set to "true" to disable Redis and use in-memory pub/sub fallback
# Note: Worker queuing requires Redis. Without it, orders won't be queued.
REDIS_DISABLED=false

DB_TYPE=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=orderdb
DB_USER=postgres
DB_PASS=postgres
